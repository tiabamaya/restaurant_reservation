<h1>Reservation Details</h1>

<p><strong>Date & Time:</strong> <%= @reservation.reserved_at.strftime("%B %d, %Y • %-I:%M %p") %></p>
<p><strong>Number of guests:</strong> <%= @reservation.party_size %></p>
<p><strong>Contact name:</strong> <%= @reservation.contact_name %></p>
<p><strong>Contact phone:</strong> <%= @reservation.contact_phone %></p>
<p><strong>Status:</strong> <%= @reservation.status %></p>

<hr>

<% if @reservation.booked? && @reservation.cancellable? %>
  <%= button_to "Cancel Reservation",
        reservation_path(@reservation),
        method: :delete,
        data: { turbo_confirm: "Cancel this reservation?" } %>
<% end %>

<p>
  <%= link_to "Back to My Reservations", reservations_path %>
</p>
